[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Vite's default port; without this, `netlify dev` waits on 3000 and times out.
[dev]
  targetPort = 5173
  framework = "#auto"
